const { formatError } = require('./errors');
const { formatSuccess } = require('./success');
const { getPrompt } = require('./prompts');

module.exports = {
    formatError,
    formatSuccess,
    getPrompt
};